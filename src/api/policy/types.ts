export interface PolicyResponse {
    id: string;
    name: string;
    status: string;
    description: string;
    startAt: string;
    endAt: string;
    createdAt: string;
    updatedAt: string;
}